/target/
desktop/src-tauri/target/
node_modules/
ide/dist/
kuni/dist/
lunisurge/dist/
.DS_Store
*.swp
.cursor/
debug-*.log
*.wasm
github_upload/
github_upload_batches/
itchupload/

# Tauri schema dumps at repo root (keep desktop/src-tauri/gen/schemas/)
/acl-manifests.json
/capabilities.json
/desktop-schema.json
/macOS-schema.json
/windows-schema.json
/linux-schema.json

# Legacy nested upload mirrors (canonical layout is flat: ide/, crates/, docs/, …)
1_root_crates/
2_ide_runtime/
3_docs_examples/
_github_upload/

# Local Netlify folder
.netlify

# Local env / secrets (Cesium ion token, Vercel token, etc.) — never commit
.env
.env.local
.env.*.local
ide/.env
ide/.env.local
kuni/.env
kuni/.env.local
lunisurge/.env
lunisurge/.env.local

# Vercel CLI link metadata (optional local)
.vercel

