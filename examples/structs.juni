struct Vec2:
    x: f32
    y: f32

fn length(v: Vec2) -> f32:
    return sqrt(v.x * v.x + v.y * v.y)

fn main() -> i32:
    let p = Vec2(x=3.0, y=4.0)
    let n = length(p)
    if n > 4.0:
        return 1
    return 0
