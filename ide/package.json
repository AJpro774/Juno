{
  "name": "juni-ide",
  "private": true,
  "version": "12.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && npm run embed:kuni",
    "embed:kuni": "npm --prefix ../kuni install && npm --prefix ../kuni run build:nested && rm -rf dist/kuni && cp -R ../kuni/dist dist/kuni",
    "preview": "vite preview",
    "build:wasm": "wasm-pack build ../crates/juni-wasm --target web --out-dir ../../ide/public/pkg --out-name juni_wasm"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.71",
    "typescript": "^7.0.2",
    "vite": "^8.1.5"
  },
  "dependencies": {
    "@mlc-ai/web-llm": "^0.2.84",
    "marked": "^18.0.7",
    "monaco-editor": "^0.55.1"
  },
  "overrides": {
    "dompurify": "3.4.12"
  }
}
