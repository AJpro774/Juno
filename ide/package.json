{
  "name": "juni-ide",
  "private": true,
  "version": "13.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && npm run embed:kuni && npm run embed:luni",
    "build:pages": "GITHUB_PAGES=true KUNI_BASE=/Juno/kuni/ LUNI_BASE=/Juno/luni/ npm run build",
    "embed:kuni": "npm --prefix ../kuni install && npm --prefix ../kuni run build:nested && rm -rf dist/kuni && cp -R ../kuni/dist dist/kuni",
    "embed:luni": "npm --prefix ../lunisurge install && npm --prefix ../lunisurge run build:nested && rm -rf dist/luni && cp -R ../lunisurge/dist dist/luni",
    "preview": "vite preview",
    "build:wasm": "wasm-pack build ../crates/juni-wasm --target web --out-dir ../../ide/public/pkg --out-name juni_wasm"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.74",
    "typescript": "^7.0.2",
    "vite": "^8.3.0"
  },
  "dependencies": {
    "@mlc-ai/web-llm": "^0.2.85",
    "marked": "^18.0.13",
    "monaco-editor": "0.55.1"
  },
  "overrides": {
    "dompurify": "3.4.15"
  }
}
